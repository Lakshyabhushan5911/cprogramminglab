#include<stdio.h>
#include<conio.h>
void main()
{
    int arr[10]'
    int i,n;
    printf("enter the size of an array");
    scanf("%d",&n);
    for(i=0;i<=n;i++)
{
    printf("\n ennter the %d value of an array);
scanf("%d",&arr[i]);
}
for(i=0;i<n;i++)
printf("\n%d %d %d %d",arr[i],*arr[i],*(i+arr),*arr+1,i[arr]);
}
}